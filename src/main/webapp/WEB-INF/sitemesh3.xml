<?xml version="1.0" encoding="UTF-8"?>

<sitemesh>

    <mapping
        path="/*"
        decorator="web.jsp" />

    <mapping
        path="/profile-image*"
        exclude="true" />

    <mapping
        path="/image*"
        exclude="true" />

    <mapping
        path="/product-image*"
        exclude="true" />

</sitemesh>